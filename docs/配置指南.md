# 配置指南

## 邮箱验证配置（管理员）

详细配置步骤请参考：[安全邮箱配置指南.md](./安全邮箱配置指南.md)

## 快速配置

1. 复制 `env.example` 为 `.env`
2. 配置邮箱 SMTP 信息（QQ邮箱推荐）
3. 配置数据库连接
4. 运行 `pnpm db:push` 迁移数据库

## 添加管理员

使用脚本：
```bash
npx tsx scripts/set-admin-password.js
```

或直接操作数据库设置用户角色为 `admin`。
